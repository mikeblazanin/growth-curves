#This file includes some packages which are dependencies for the main script
# but which are suggested dependencies and not loaded with library(), so renv
# fails to capture them with snapshot()

library(GGally)
library(ggforce)